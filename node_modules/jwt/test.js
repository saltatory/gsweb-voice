var jwt = require('./lib/jwt');
var payload = {'scope':''}, token;
payload.iss = 'APP_KEY';
payload.exp = Math.round(new Date().getTime()/1000) + 3600;
        
token = new jwt.WebToken(JSON.stringify(payload), JSON.stringify({typ:'JWT', alg: 'HS256'}));
console.log(token.serialize('abc'));